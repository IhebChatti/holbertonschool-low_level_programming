#include "holberton.h"
/**
*print_last_digit - prints the last digit of a number
*@int: integer that returns the last digit
*Return: res
*/
int print_last_digit(int)
{
	int n, lastDigit;

	lastDigit = (n % 10);
	return (lastDigit);
}
