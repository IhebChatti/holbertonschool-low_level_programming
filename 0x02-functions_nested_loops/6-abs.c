#include "holberton.h"
/**
*_abs - print the absolute value of an integer
*@int: the integer that will give the absolute
*Return: 0
*/
int _abs(int)
{
	int a;

	if (a < 0)
	{
		return (-(a));
	}
	else
	{
		return (a);
	}
}
