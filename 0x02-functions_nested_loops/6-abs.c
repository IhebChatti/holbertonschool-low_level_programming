#include "holberton.h"
/**
*_abs - print the absolute value of an integer
*@a: the integer that will give the absolute
*Return: 0
*/
int _abs(int a)
{
	if (a < 0)
	{
		a = -a;
		return (a);
	}
	else
	{
		return (a);
	}
}
